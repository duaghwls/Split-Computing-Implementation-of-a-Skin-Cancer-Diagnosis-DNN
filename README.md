# Split Computing 기반 피부암 진단 시스템

> 🎓 학부 종합설계 프로젝트 | Split Computing을 활용한 분할 딥러닝 추론 시스템

## 🔗 빠른 링크

- 📖 **[빠른 시작 가이드](QUICKSTART.md)** - 5분 안에 시작하기
- 📚 **[상세 사용법](docs/USAGE_GUIDE.md)** - 설치부터 실행까지
- 🏗️ **[아키텍처 문서](docs/ARCHITECTURE.md)** - 시스템 구조 이해하기
- ✅ **[GitHub 업로드 체크리스트](GITHUB_CHECKLIST.md)** - 업로드 가이드
- 📋 **[정리 보고서](docs/CLEANUP_REPORT.md)** - 변경사항 요약

---

## 📌 프로젝트 개요

본 프로젝트는 Split Computing 기술을 활용하여 딥러닝 모델을 클라이언트-서버 환경에서 분할 추론하는 피부암 진단 시스템입니다. InceptionResNetV2 모델을 두 부분으로 분할하여, 클라이언트에서는 모델의 앞부분(head)을, 서버에서는 뒷부분(tail)을 실행함으로써 효율적인 추론을 구현했습니다.

## 🎯 주요 기능

- **모델 분할(Model Splitting)**: InceptionResNetV2를 head와 tail로 분할
- **클라이언트-서버 아키텍처**: Socket 통신을 통한 중간 결과 전송
- **피부암 진단**: ISIC 데이터셋을 활용한 악성/양성 종양 분류
- **효율적인 추론**: 클라이언트의 연산 부담을 줄이고 서버에서 무거운 연산 처리

## 🏗️ 프로젝트 구조

```
.
├── src/
│   ├── client/              # 클라이언트 코드
│   │   ├── client_test.py   # 클라이언트 추론 코드
│   │   └── socket_client_SC.py  # 소켓 클라이언트
│   ├── server/              # 서버 코드
│   │   └── server_test.py   # 서버 추론 코드
│   ├── training/            # 모델 학습 코드
│   │   └── SC_modelSave.py  # 모델 학습 및 저장
│   └── utils/               # 유틸리티 함수
│       └── splitter.py      # 모델 분할 유틸리티
├── assets/                  # 이미지 및 시각화 자료
│   ├── head_structure.png   # Head 모델 구조
│   ├── tail_structure.png   # Tail 모델 구조
│   └── original_structure.png  # 원본 모델 구조
├── docs/                    # 문서 및 발표 자료
│   ├── 중간발표_script.docx
│   └── 종합설계 포스터.pdf
├── models/                  # 학습된 모델 저장 (gitignore 처리)
├── data/                    # 데이터셋 (gitignore 처리)
├── README.md
├── requirements.txt
└── .gitignore
```

## 🛠️ 기술 스택

- **Framework**: TensorFlow 2.x, Keras
- **Model**: InceptionResNetV2 (ImageNet pretrained)
- **Communication**: Python Socket
- **Dataset**: ISIC (International Skin Imaging Collaboration)
- **Language**: Python 3.x

## 📋 요구사항

```bash
pip install -r requirements.txt
```

주요 라이브러리:
- tensorflow
- keras
- numpy
- pillow
- pickle

## 🚀 사용 방법

### 1. 모델 학습

```bash
cd src/training
python SC_modelSave.py
```

### 2. 서버 실행

```bash
cd src/server
python server_test.py
```

### 3. 클라이언트 실행

```bash
cd src/client
python client_test.py
```

## 📊 모델 아키텍처

본 프로젝트는 InceptionResNetV2 모델을 `block8_1` 레이어를 기준으로 분할합니다:

- **Head Model** (클라이언트): 
  - 입력 레이어부터 `block8_1`까지
  - 클라이언트에서 실행
  
- **Tail Model** (서버):
  - `block8_1` 이후부터 출력 레이어까지
  - GlobalAveragePooling2D
  - Dropout (0.3)
  - Dense (128, relu, L2 regularization)
  - Dropout (0.3)
  - Dense (2, sigmoid)

## 🔄 워크플로우

1. 클라이언트가 이미지를 입력받아 전처리 (150x150, normalization)
2. Head 모델로 중간 특징 추출
3. Socket을 통해 서버로 중간 결과 전송
4. 서버가 Tail 모델로 최종 추론 수행
5. 결과를 클라이언트에게 반환 (악성/양성 판정)

## 📝 주요 파일 설명

- **`splitter.py`**: 원본 모델을 head와 tail로 분할하는 유틸리티
- **`SC_modelSave.py`**: InceptionResNetV2 기반 모델 학습 및 저장
- **`client_test.py`**: 클라이언트에서 head 추론 및 결과 전송
- **`server_test.py`**: 서버에서 tail 추론 및 최종 결과 반환
- **`socket_client_SC.py`**: Socket 클라이언트 통신 모듈

## ⚠️ 주의사항

- 학습된 모델 파일(`SC_best_model_InceptionResNetV2.h5`)은 용량이 커서 Git에 포함되지 않습니다.
- 클라이언트와 서버의 IP 주소 및 포트 번호를 환경에 맞게 수정해야 합니다.
- ISIC 데이터셋은 별도로 다운로드 받아 `data/` 폴더에 배치해야 합니다.

## 🎓 학습 성과

본 프로젝트는 학부 종합설계 프로젝트로 진행되었으며, Split Computing을 통해:
- 클라이언트 연산 부담 감소
- 모바일 디바이스에서도 딥러닝 추론 가능
- 네트워크 통신을 통한 분할 처리 구현

## 📄 라이센스

본 프로젝트는 교육 목적으로 작성되었습니다.

## 👥 기여자

- 프로젝트 작성자: 염호진, 김경운
- 작성 기간: 2023년
